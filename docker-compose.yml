version: '3.8'

services:
  web:
  # Baut das Docker-Image aus der Dockerfile im aktuellen Verzeichnis
    build: .
      # Ordnet Port 8081 des Hosts auf Port 80 des Containers ab
    ports:
      - "8081:80"

    # Startet den Container automatisch neu, falls er abstürzt
    restart: always
